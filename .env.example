# Since the ".env" file is gitignored, you can use the ".env.example" file to
# build a new ".env" file when you clone the repo. Keep this file up-to-date
# when you add new variables to `.env`.

# This file will be committed to version control, so make sure not to have any
# secrets in it. If you are cloning this repo, create a copy of this file named
# ".env" and populate it with your secrets.

# When adding additional environment variables, the schema in "/src/env.js"
# should be updated accordingly.

# =============================================================================
# DATABASE CONFIGURATION
# =============================================================================

# Development Database (for local development)
# Create your own dev database: `turso db create your-project-dev`
# Get the URL: `turso db show your-project-dev`  
# Get auth token: `turso db tokens create your-project-dev`
DATABASE_URL_DEV="libsql://your-project-dev-username.aws-region.turso.io"
DATABASE_AUTH_TOKEN_DEV="your-development-database-auth-token"

# Production Database (only needed for production deployment)
# For local development, you can use placeholder values - they won't be used
# Real values are only needed in production environment (Vercel, etc.)
DATABASE_URL_PROD="libsql://placeholder-prod-url"
DATABASE_AUTH_TOKEN_PROD="placeholder-prod-token"

# =============================================================================
# IPFS SERVICE (PINATA)
# =============================================================================

# Get your JWT token from: https://app.pinata.cloud/keys
PINATA_JWT="your-pinata-jwt-token"

# =============================================================================
# SIGNATURE SERVICE
# =============================================================================

# Development Environment Signer (for local development)
# Generate with: openssl rand -hex 32 (then add 0x prefix)
SIGNER_PRIVATE_KEY_DEV="0x1234567890abcdef1234567890abcdef1234567890abcdef1234567890abcdef"
NEXT_PUBLIC_SIGNER_ADDRESS_DEV="0x1234567890123456789012345678901234567890"

# Production Environment Signer (only needed for production deployment)
# For local development, you can use placeholder values - they won't be used
SIGNER_PRIVATE_KEY_PROD="0xplaceholder1234567890abcdef1234567890abcdef1234567890abcdef123456"
NEXT_PUBLIC_SIGNER_ADDRESS_PROD="0xplaceholder1234567890123456789012345678" 